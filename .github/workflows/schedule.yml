name: Scheduled Job

on:
  schedule:
    # Runs every day at 4:06 AM IST
    - cron: "36 22 * * *"
  workflow_dispatch:

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "This runs every day at 4:06 AM IST"
