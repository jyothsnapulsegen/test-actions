name: Scheduled Job

on:
  schedule:
    # Runs every day at 4:06 PM IST
    - cron: "36 10 * * *"
  workflow_dispatch:

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "This runs every day at 4:06 PM IST"
