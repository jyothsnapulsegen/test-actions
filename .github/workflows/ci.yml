name: CI Pipeline

on:
  push:

jobs:

  hello:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Hello from CI!"

  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Running tests..."

  build:
    needs: hello
    runs-on: ubuntu-latest

    steps:
      - run: echo "Building application..."
